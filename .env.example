# EZRouting Test Automation Credentials
# Copy this file to .env and fill in your credentials
# IMPORTANT: Never commit .env to version control (already in .gitignore)

# Required: User credentials for testing
AUTOMATION_SUPER_USER=your-email@example.com
AUTOMATION_SUPER_PASSWORD=your-password-here

# Optional: Test environment (DEV, UAT, PROD, QA)
# Determines which environment URLs to use
# DEV: https://routing-dev.transact.com
# UAT: https://routing-uat.transact.com (default)
# QA: https://routing-qa.transact.com
# PROD: https://www.ezrouting.com
ENV=UAT

# Optional: Override base URL (takes precedence over ENV)
# EZROUTING_BASE_URL=https://routing-uat.transact.com/testqa

# Default district/client (used for Playwright tests; defaults to "testqa" if omitted)
CLIENT=testqa

# Optional: Slack webhook URL for test notifications
# Get webhook URL from: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_DEBUG_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
